"use client";
import React from "react";
import { ToastContainer } from "react-toastify";

const SnackBar = () => {
  return (
    <div>
      <ToastContainer />
    </div>
  );
};

export default SnackBar;
